Docker PHP environments
=

Checkout the branch you want to use for (7.2):
```sh
git checkout -b php72-xdebug origin/php72-xdebug
```

Build the image for (7.2):
```sh
docker build -t php72-xdebug .
```

